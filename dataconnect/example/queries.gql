mutation InsertOrganization {
  organization_insert(
    data: {name: "NewCo", createdAt: "now()"}
  )
}

mutation UpdateTask($id: UUID!, $status: String!) {
  task_update(
    id: $id,
    data: {status: $status}
  )
}

query ListProjects {
  projects {
    id
    name
    description
  }
}

query ListUsersByOrganization($organizationId: UUID!) {
  users(where: {organizationId: {eq: $organizationId}}) {
    id
    displayName
    email
  }
}
