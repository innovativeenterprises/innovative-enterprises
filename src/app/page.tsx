export default function Home() {
  // Return a simple div to ensure the root path renders a valid page,
  // preventing the 404 error. The background color will be inherited
  // from the main layout.
  return <div className="h-screen w-full"></div>;
}
